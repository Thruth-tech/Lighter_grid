# Python dependencies for Lighter Grid Trading Bot

# Environment variable management
python-dotenv==1.0.0

# HTTP requests
requests==2.31.0

# Lighter Python Client
# Install with: pip install git+https://github.com/elliottech/lighter-v2-python.git
lighter-v2-python @ git+https://github.com/elliottech/lighter-v2-python.git

# Async support (built-in for Python 3.7+, but included for compatibility)
# asyncio is built-in, no need to install
